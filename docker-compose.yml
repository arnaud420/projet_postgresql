version: '3'
services:
    database:
        image: mariadb
        restart: always
        environment:
            - MYSQL_ROOT_PASSWORD=password
            - MYSQL_DATABASE=test
        ports:
            - 127.0.0.1:3306:3306

